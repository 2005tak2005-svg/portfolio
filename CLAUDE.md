# คำแนะนำสำหรับ Claude ที่มาทำงานกับ repo นี้

อ่านไฟล์นี้ให้จบก่อนแก้อะไร

## repo นี้คืออะไร

หน้าเว็บ portfolio หน้าเดียวจบ ภาษาไทย สำหรับส่งลิงก์ให้ HR เปิดดู
เว็บจริงอยู่ที่ https://2005tak2005-svg.github.io/portfolio/

```
index.html            เนื้อหาทั้งหมดอยู่ในไฟล์นี้ไฟล์เดียว
assets/css/style.css  สี ตัวอักษร การจัดวาง (ตัวแปรทั้งหมดอยู่ใน :root ข้อ 1)
assets/css/fonts.css  @font-face ของฟอนต์ที่เก็บไว้ในโปรเจกต์เอง
assets/js/main.js     อนิเมชันตอนเลื่อน + กดดูภาพขยาย ~90 บรรทัด
assets/fonts/         ฟอนต์ .woff2 (OFL) — ห้ามลบ
images/               รูปภาพ — อ่าน images/README.md ก่อนใส่รูป
```

**ไม่มีขั้นตอน build ไม่มี Node ไม่มี dependency และจะไม่มี**
ถ้ารู้สึกว่าอยากเพิ่ม framework, bundler, Tailwind, หรือ npm package — อย่า
งานที่ต้องทำกับหน้านี้ทำได้ด้วย HTML/CSS/JS ล้วนทั้งหมด

## กฎการออกแบบ — ข้อนี้สำคัญที่สุด

**อัปเดต 2569-08-12:** เจ้าของ repo ขอเปลี่ยนทิศทางหน้าตาใหม่ทั้งเว็บ
โดยอ้างอิงเทมเพลตแฟชั่นโทนทอง/ครีม (วงกลม โค้งครึ่งวงกลม ซุ้มโค้งด้านบนของรูป
ปุ่มแคปซูลสีเข้ม ตัวหนังสือหัวเรื่องแบบ uppercase เว้นระยะห่าง) — ดูตัวอย่างที่ทำแล้ว
ใน hero section (`#top`) และหัวเว็บ (`.site-header`) เป็นต้นแบบของทิศทางใหม่นี้
กฎเก่าด้านล่างที่เคยห้าม border-radius เกิน 2px, ห้ามสีใหม่นอกตัวแปรเดิม
**ถูกยกเลิกแล้ว** สำหรับหมวดที่ปรับใหม่ — ให้ทำหมวดอื่นที่เหลือ (ผลงาน, เครื่องมือ
ที่สร้างเอง, ทักษะ, ไทม์ไลน์, เกี่ยวกับผม, ติดต่อ) ให้กลมกลืนไปในทิศทางเดียวกัน
เมื่อได้รับมอบหมาย โดยอิงพาเลตและคอมโพเนนต์ที่เพิ่มใหม่ด้านล่าง

**ยังคงห้ามเหมือนเดิม** (ข้อเหล่านี้ไม่เกี่ยวกับการเปลี่ยนทิศทาง)

- gradient แบบไล่เฉดสี (สีตันหลายก้อนประกอบเป็นรูปทรง เช่นวงกลม/ครึ่งวงกลม ทำได้ปกติ)
- glassmorphism, `backdrop-filter`, การ์ดโปร่งแสง
- `box-shadow` บนการ์ด, เงาเรืองแสง, การ์ดที่ลอยขึ้นตอน hover
- emoji แทนไอคอน
- ฟอนต์ใหม่ (มีอยู่แล้ว 2 ตัวคือ Noto Serif Thai กับ IBM Plex Sans Thai — ปรับ
  น้ำหนัก/ระยะห่างตัวอักษร/uppercase ของฟอนต์เดิมได้ ไม่ต้องโหลดฟอนต์เพิ่ม)
- อนิเมชันเพิ่ม เช่น ตัวเลขวิ่ง, พิมพ์ดีด, scroll-jacking, ไลบรารีอนิเมชัน
  (**อัปเดต 2569-08-25:** parallax เบา ๆ อนุญาตแล้ว แต่**เฉพาะ**กับของตกแต่ง
  `.deco` / `.shape` เท่านั้น — ดูหัวข้อ "parallax ของตกแต่ง" ด้านล่าง
  ห้ามใช้ parallax กับตัวหนังสือหรือรูปเนื้อหา (portrait, gallery, logo)

**พาเลตสี (อัปเดตแล้ว ใน `:root`)**

- โทนเดิม: `--paper` `--paper-deep` `--ink` `--ink-soft` `--ink-mute` `--rule`
- โทนใหม่: `--accent` (เปลี่ยนจากแดงเลือดหมูเป็นทอง-น้ำตาล), `--gold`, `--gold-deep`
  (วงกลม/ปุ่ม hover), `--stone`, `--stone-soft` (รูปทรงตกแต่งโทนเทาอมน้ำตาล)
- ยังคงห้ามฮาร์ดโค้ดค่าสี hex ใหม่นอกตัวแปรเหล่านี้ — ถ้าต้องการโทนเพิ่ม ให้เพิ่ม
  เป็นตัวแปรใน `:root` ก่อน

**คอมโพเนนต์ใหม่ที่เพิ่มเข้ามา**

- `.btn-pill` ปุ่มแคปซูลพื้นเข้ม ใช้กับ call-to-action หลักของหน้าเท่านั้น
  (อย่าใช้พร่ำเพรื่อ — หน้านี้ยังคงเน้นลิงก์ขีดเส้นใต้ `.link-arrow` เป็นหลัก)
- `.shape`, `.shape--circle`, `.shape--half-a`, `.shape--stack`, `.shape--arcs`
  รูปทรงตกแต่ง (วงกลม/ครึ่งวงกลม/บันได/วงแหวนซ้อน) ใช้ `aria-hidden="true"`
  เสมอเพราะเป็นของตกแต่งล้วน ไม่ใช่เนื้อหา
- `.monogram` ตราวงกลมอักษรย่อในหัวเว็บ, `.nav-icon` ไอคอนแฮมเบอร์เกอร์ตกแต่ง
  (ยังไม่ใช่เมนูที่กดได้จริง — เมนูจริงคือ `.site-nav`)

**parallax ของตกแต่ง (เพิ่ม 2569-08-25)**

`.deco` และ `.shape` ทุกตัวเลื่อนเบา ๆ ตามตำแหน่งบนจอโดยอัตโนมัติ (ดู
ส่วน "5. parallax เบา ๆ" ใน `assets/js/main.js`) — ไม่ต้องเพิ่ม data attribute
หรือคลาสอะไรเพิ่ม แค่ประกาศ `.deco`/`.shape` ตามปกติก็ได้ผลเลย
ระยะเลื่อนคุมด้วยตัวแปร `DRIFT_PX` ในสคริปต์ (ค่าเริ่มต้น ±18px) — ปรับได้
แต่อย่าให้เกิน ~24px มิเช่นนั้นจะเริ่มดูรก ไม่ใช่มินิมอลอีกต่อไป
เคารพ `prefers-reduced-motion` อยู่แล้ว (ใช้ตัวแปร `reduced` ตัวเดียวกับ
ส่วน data-reveal) — ปิด parallax ให้อัตโนมัติถ้าผู้ใช้ตั้งค่าลดการเคลื่อนไหวไว้

**ยังใช้เหมือนเดิม**

- เส้นคั่นบาง `1px solid var(--rule)` สำหรับเนื้อหาที่เป็นตัวหนังสือล้วน (builds, tools)
- ลำดับความสำคัญสร้างด้วยขนาดและน้ำหนักตัวอักษรเป็นหลัก ผสมสี/รูปทรงใหม่ได้แต่อย่าล้น
- คลาสที่มีอยู่แล้ว: `.measure` (คุมความยาวบรรทัด), `.link-arrow` (ลิงก์มีเส้นใต้),
  `.shell` (คุมความกว้าง), `data-reveal` (ค่อย ๆ โผล่ตอนเลื่อนถึง)

ถ้าจะเพิ่ม CSS ใหม่ ให้เขียนต่อจากบล็อกที่ใกล้เคียงที่สุด และใช้ตัวแปรเดิม
ไฟล์ CSS แบ่งเป็นข้อ 1–14 มีคอมเมนต์กำกับหัวข้อไว้แล้ว เขียนคอมเมนต์ภาษาไทยให้เหมือนกัน

## โครงหน้าเว็บ

| หมวด | id | ใช้ทำอะไร |
| --- | --- | --- |
| Hero | `#top` | ชื่อ ตำแหน่ง แนะนำตัวสั้น ๆ |
| ผลงาน | `#work` | งานที่**มีรูป** — แกลเลอรี |
| เครื่องมือที่สร้างเอง | `#builds` | เครื่องมือที่เขียนเอง **ไม่มีรูป** |
| เครื่องมือที่ใช้จริง | `#tools` | รายการทักษะสั้น ๆ |
| เส้นทางและกิจกรรม | `#timeline` | ไทม์ไลน์ |
| เกี่ยวกับผม | `#about` | ประวัติย่อ |
| ติดต่อ | `#contact` | อีเมล ลิงก์ |

`#builds` กับ `#tools` **คนละหมวดกัน** อย่าสับสน
`#builds` คือ "ของที่ผมสร้าง" ส่วน `#tools` คือ "ของที่ผมใช้เป็น"

## วิธีเพิ่มเนื้อหา

ทั้งสองหมวดมี **บล็อกตัวอย่างในคอมเมนต์** อยู่ท้ายหมวดแล้ว ก๊อปไปวางแล้วแก้ค่า
อย่าประดิษฐ์โครงสร้าง HTML ใหม่เอง

**เครื่องมือที่สร้างเอง (`#builds`)** — ใช้ `<li class="build">`
ตั้งใจให้เล่าด้วยตัวหนังสือล้วน เพราะ CLI และสคริปต์ไม่มีอะไรให้ถ่ายรูป
นี่ไม่ใช่การ์ดที่รูปหาย อย่าพยายามยัดกรอบรูปเข้าไป
เขียน `build__desc` จาก**มุมของปัญหา** ไม่ใช่มุมของโค้ด — HR อ่านแล้วต้องเข้าใจว่า
เครื่องมือนี้ทำให้ชีวิตใครง่ายขึ้นยังไง ไม่ใช่ว่าใช้ library อะไร
ถ้ายังไม่มีลิงก์โค้ดให้ดู ให้ลบบรรทัด `build__links` ทิ้ง อย่าใส่ `href="#"` ค้างไว้

**ผลงาน (`#work`)** — ใช้ `<article class="work">` สำหรับงานที่มีรูปเท่านั้น
คุมขนาดด้วย `data-span` ได้ 4 แบบ: `full` / `wide` / `half` / `narrow`
(`wide` กับ `narrow` ออกแบบมาให้วางคู่กันพอดีบนจอกว้าง)
จังหวะของแกลเลอรีตั้งใจให้ไม่เท่ากัน อย่าแก้ให้เป็นตารางเท่ากันหมด
วิธีใส่ไฟล์รูปอ่านใน `images/README.md`

## เรื่องข้อความ

ข้อความที่อยู่ในวงเล็บ `‹ … ›` คือ**ช่องว่างที่ยังไม่ได้เติม**

- ห้ามปล่อย `‹ … ›` ค้างไว้ในส่วนที่แก้แล้ว
- **ห้ามแต่งข้อมูลส่วนตัวขึ้นเอง** — ชื่อ อายุ ที่ทำงาน วุฒิการศึกษา ปี พ.ศ.
  ถ้าไม่รู้ ให้เว้น `‹ … ›` ไว้ตามเดิม แล้วบอกเจ้าของ repo ว่าต้องเติมตรงไหนบ้าง
  การเดาข้อมูลในเรซูเม่คือการโกหกนายจ้าง ไม่ใช่การเติมช่องว่าง
- ปีใช้ พ.ศ. ให้ตรงกับที่อื่นในหน้า
- เขียนให้กระชับ HR อ่านหน้านี้ไม่เกินหนึ่งนาที

## เรื่องความปลอดภัย

เวลาเอาข้อมูลเครื่องมือจาก repo อื่นมาเขียนลงหน้านี้ **อย่าคัดลอกมาทั้งดุ้น**
ห้ามให้สิ่งเหล่านี้หลุดขึ้นเว็บสาธารณะ

- API key, token, รหัสผ่าน, connection string
- ชื่อโฮสต์ภายใน, path ในเครื่อง, ชื่อฐานข้อมูล
- ชื่อลูกค้าหรือข้อมูลลูกค้า ถ้าไม่ได้ขออนุญาต

หน้านี้เป็น public repo และ public website ทุกอย่างที่ commit คือทุกอย่างที่คนทั้งโลกเห็น

## ตรวจงานก่อน commit

```bash
python3 -m http.server 8000   # แล้วเปิด http://localhost:8000
```

เช็คให้ครบ 4 ข้อ

1. ลองย่อ-ขยายหน้าต่างที่ความกว้าง 390 / 768 / 1440 — **ต้องไม่มีแถบเลื่อนแนวนอน**
2. เปิด console — ต้องไม่มี error
3. เลื่อนหน้าให้สุด — ทุกอย่างต้องโผล่ครบ ไม่มีอะไรค้างจาง ๆ อยู่
4. ปิด JavaScript แล้วโหลดใหม่ — เนื้อหาต้องอ่านได้ครบ (แค่ไม่มีอนิเมชัน)

ถ้ามี Playwright ใช้ได้ ยิงสกรีนช็อตทั้ง 3 ความกว้างแล้วดูด้วยตาอีกที
หมวดใหม่ต้องดูกลมกลืนกับหมวดเดิม ไม่ใช่ดูเหมือนของที่แปะเพิ่มเข้ามา

## การ deploy

push เข้า `main` แล้วจบ — `.github/workflows/pages.yml` จะเอาขึ้นเว็บให้เองใน 1–2 นาที

**อย่าไปเปลี่ยน Settings → Pages กลับไปเป็น "Deploy from a branch"**
โหมดนั้น GitHub จะรัน Jekyll ทับ ซึ่งข้ามไฟล์ที่ขึ้นต้นด้วย `_`
และมีปัญหากับชื่อไฟล์ภาษาไทย เว็บนี้เป็น HTML ล้วน ไม่ต้อง build อะไรเลย
