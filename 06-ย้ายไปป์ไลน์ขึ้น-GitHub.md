# 06 — ย้ายไปป์ไลน์ (Phase 1 + Phase 2) ขึ้น GitHub ให้เรียกจากเครื่องไหนก็ได้

> เขียนต่อจากชุดเอกสารเดิม (`01`–`05`) — อ่านคู่กับ `02-สภาพแวดล้อมและการติดตั้ง.md` และ
> `03-สกิลและเอเจนต์ทั้งไปป์ไลน์.md` เพราะเอกสารนี้จะอ้าง path/ไฟล์ที่นั่นตลอด
>
> **สรุปหัวใจ:** ตอนนี้ไปป์ไลน์ผูกกับ "เครื่อง Mac เครื่องนี้เครื่องเดียว" อยู่ 4 ชั้น — path,
> เครื่องมือ/ฮาร์ดแวร์, ข้อมูล, และ **เซสชันเบราว์เซอร์ที่ล็อกอินค้างไว้** ย้ายขึ้น GitHub แก้ได้
> 3 ชั้นแรกเกือบทั้งหมด แต่ชั้นที่ 4 (ดาวน์โหลด replay จาก TikTok) **ย้ายออกจากเครื่องที่มี Chrome
> ล็อกอินอยู่ไม่ได้จริง** ไม่ว่าโค้ดจะอยู่ที่ไหนก็ตาม — รายละเอียดอยู่ข้อ 4

---

## 1. อะไรผูกกับเครื่องนี้อยู่ตอนนี้ (ตรวจจริงจาก `02`/`03`)

| จุดที่ผูกกับเครื่อง | อาการตอนย้ายเครื่อง |
| --- | --- |
| `/Volumes/PortableSSD/CLAUDE edit` (path มีช่องว่าง, exFAT) | เครื่องอื่น mount ชื่อ volume คนละชื่อ/คนละ drive letter → path พังทันที |
| เครื่องมือผ่าน Homebrew (`ffmpeg`, `whisper-cli`, `yt-dlp` ที่ `/opt/homebrew/bin`) | มีเฉพาะ macOS/Apple Silicon — เครื่อง Windows ไม่มี `/opt/homebrew` เลย |
| โมเดล Whisper `ggml-large-v3-turbo-q5_0.bin` (547 MB) วางไว้บน SSD ตรงๆ | ไม่ได้อยู่ใน git (ใหญ่เกิน) และไม่มีสคริปต์ติดตั้งอัตโนมัติในเครื่องใหม่ |
| `h264_videotoolbox` (ตัวเร่ง re-encode ตอนตัดคลิป) | เป็นของ Apple Silicon เท่านั้น เครื่อง Windows/Intel ต้อง fallback เป็น `libx264` (ช้ากว่ามาก) |
| Whisper เร่งด้วย Metal | ทำงานได้เฉพาะ Apple Silicon — เครื่องอื่นถอดเสียงช้ากว่าตัวเลขในข้อ 4 ของ `02` มาก |
| Skills/Agents อยู่ที่ `~/.claude/skills/`, `~/.claude/agents/` (ระดับผู้ใช้ ไม่ใช่ระดับโปรเจกต์) | `02` ยืนยันแล้วว่าโฟลเดอร์ทำงานทั้งหมด **"ไม่ใช่ repo"** — ไม่มี version control เลย |
| GSAP โหลดจาก CDN (`cdn.jsdelivr.net`) ไม่ vendor | ต้องมีเน็ตตอนเรนเดอร์ ถ้าเน็ตหลุด **ฉากไม่ขยับแต่ไม่ error** (บั๊กเงียบ) |
| HyperFrames ต้อง pin เวอร์ชัน (`0.7.76`/`0.7.56`) | เครื่องใหม่ที่ `npx hyperframes@latest` จะพังตามที่ `02` เจอมาแล้ว |
| `TikTok_Live_Tracker.xlsx` เป็นไฟล์เดี่ยวบน SSD | ถ้ารันจากสองเครื่องโดยไม่ sync ไฟล์ → ข้อมูลแตกเป็นสองเวอร์ชัน (fork) |
| Chrome ตั้งค่า auto-save ไป `/LIve/` + ล็อกอิน TikTok ค้างไว้ | เป็นการตั้งค่าเฉพาะเครื่องนี้เครื่องเดียว ย้ายไปเครื่องอื่นต้องตั้งใหม่ทั้งหมด |
| MCP server ชื่อ Notion จริงในเครื่องคือ `mcp__claude_ai_Notion__*` (ไม่ตรงกับที่ hardcode ไว้ในร่าง `trend-scout`) | ชื่อเซิร์ฟเวอร์ MCP เปลี่ยนได้ตามเครื่อง/เซสชัน — hardcode แล้วเปราะ |

จุดที่ **ออกแบบดีอยู่แล้วและย้ายง่าย**: `vu.mjs` มีลำดับค้นหาแบบ env var → SSD → home directory
(`VU_CACHE_DIR`, `VU_WHISPER_MODEL_DIR`, `VU_WHISPER_MODEL`) อยู่แล้ว นี่คือรูปแบบที่ควรใช้ซ้ำกับ
สคริปต์อื่นทั้งหมด

---

## 2. โครงสร้าง repo ที่แนะนำ — แยก "โค้ด" ออกจาก "ข้อมูล" และ "ความลับ"

```
n-marine168-pipeline/                  ← repo บน GitHub
├── .claude/
│   ├── skills/                        ← live-daily-cut, video-understand, (Phase2 skills เมื่อสร้างแล้ว)
│   └── agents/                        ← tiktok-replay-fetch.md, tiktok-replay-await.md, tiktok-tracker-update.md, trend-scout.md
├── scripts/
│   ├── vu.mjs, wait-for-download.mjs  ← Node เพียวๆ ไม่มี dependency (ย้ายได้ตรงๆ)
│   └── setup/
│       ├── install-mac.sh             ← brew install ffmpeg whisper-cpp yt-dlp + ดาวน์โหลดโมเดล
│       ├── install-windows.ps1        ← winget/scoop เทียบเท่า + ดาวน์โหลดโมเดล
│       └── verify-doctor.mjs          ← เวอร์ชัน cross-platform ของ `vu.mjs doctor`
├── config/
│   ├── .env.example                   ← เทมเพลตตัวแปร (ไม่มีค่าจริง)
│   └── pinned-versions.json           ← { "hyperframes": "0.7.76", "gsap": "3.15.0" }
├── assets/vendor/gsap.min.js          ← GSAP วางไฟล์จริงไว้ใน repo ไม่ดึงจาก CDN
├── docs/                              ← เอกสารชุด 01–06 นี้ทั้งหมด (ย้ายจาก SSD มาไว้ในนี้)
└── README.md                          ← "clone แล้วทำอะไรก่อน" (ข้อ 5 ด้านล่าง)
```

**สิ่งที่ห้ามใส่ใน git:**
- โมเดล Whisper (547 MB) — ใหญ่เกิน ให้ `install-*.sh/.ps1` ดาวน์โหลดจาก Hugging Face แทน (คำสั่ง
  `curl` เดิมใน `01-คู่มือติดตั้ง.md` ใช้ต่อได้เลย)
- วิดีโอ/เสียงต้นฉบับ, คลิปที่ตัดแล้ว, รูปสินค้า — เก็บบน SSD/cloud storage ตามเดิม อ้างผ่าน env var
- `TikTok_Live_Tracker.xlsx` ตัวจริง, ไฟล์ `.env`, Notion token, ฟอนต์ที่ติด license — ทั้งหมดนี้เป็น
  "ข้อมูล" หรือ "ความลับ" ไม่ใช่ "โค้ด"

---

## 3. รายจุดที่ต้องแก้จริงก่อนใช้ข้ามเครื่องได้

### 3.1 Path & ตัวแปรสภาพแวดล้อม
ทำทุกสคริปต์ให้เป็นแบบเดียวกับ `vu.mjs` คือไม่ hardcode `/Volumes/PortableSSD/...` แต่อ่านจาก
`.env` (ที่ไม่ commit) เช่น `PIPELINE_ROOT=/Volumes/PortableSSD/CLAUDE edit` บนเครื่อง Mac หรือ
`PIPELINE_ROOT=D:\pipeline-data` บนเครื่อง Windows แล้ว **ทุก path ในสคริปต์ต่อจากตัวแปรนี้** สคริปต์
ที่ยังไม่มี env var ให้ตรวจก่อน (เช่น `wait-for-download.mjs`, `tiktok-tracker-update.md` ที่อ้าง path
ตรงๆ)

### 3.2 ติดตั้งเครื่องมือข้าม OS
`ffmpeg` / `whisper-cli` (whisper.cpp) / `yt-dlp` ไม่มีทางเลี่ยงว่าต้องติดตั้งต่อเครื่อง (ไม่ใช่ไฟล์ที่
commit ลง git ได้อยู่แล้วเพราะเป็นไบนารีระบบ) ทางเลือก:
- เขียนสคริปต์ติดตั้งแยกราย OS (`install-mac.sh` ใช้ Homebrew ตามเดิม, `install-windows.ps1` ใช้
  `winget`/`scoop`) แล้วให้ทุกเครื่อง "ติดตั้งครั้งแรก" ก่อนใช้งาน
- หรือห่อด้วย Docker container (แก้ปัญหาข้าม OS ได้เบ็ดเสร็จกว่า แต่ **เสียประโยชน์การเร่งด้วย
  ฮาร์ดแวร์ของ Apple Silicon** — `h264_videotoolbox` และ Metal ใช้ในคอนเทนเนอร์ไม่ได้ ต้องยอมรับว่า
  ความเร็วจริงจะไม่เท่าตัวเลขในข้อ 4 ของ `02` อีกต่อไป)

### 3.3 ตัวเร่งฮาร์ดแวร์ต้องมี fallback
`h264_videotoolbox` ใช้ได้เฉพาะ Apple Silicon สคริปต์ตัดคลิปต้อง **ตรวจว่ามีตัวเร่งนี้ไหมก่อนใช้**
แล้วสลับไป `libx264` อัตโนมัติเมื่อไม่มี (ช้าลงมาก แต่ยังรันได้) เช่นเดียวกับ Whisper ที่ควรตรวจว่า
มี Metal ไหมก่อนตั้งความคาดหวังเรื่องความเร็ว

### 3.4 Vendor ไฟล์ที่พึ่ง CDN
GSAP ที่ดึงจาก `cdn.jsdelivr.net` (แจ้งไว้ใน `02` ข้อ 5) ให้คัดลอกไฟล์จริงเข้า
`assets/vendor/gsap.min.js` ใน repo แล้วอ้าง relative path แทนทุกจุด (มีไฟล์ vendored อยู่แล้วใน
`hyperframes-project/.agents/skills/*/vendor/gsap.min.js` — ใช้ไฟล์นั้นเป็นต้นทางได้เลย ไม่ต้องโหลดใหม่)

### 3.5 ล็อกเวอร์ชันไว้ในไฟล์ ไม่ใช่ในความจำ
`hyperframes@0.7.76` ต้องถูก pin ไว้ใน `package.json`/`config/pinned-versions.json` ที่ commit ลง
git ไม่ใช่แค่จำไว้ว่า "ห้ามใช้ latest" — เครื่องใหม่ที่ clone repo แล้วรัน `npm install` ต้องได้เวอร์ชัน
เดิมเป๊ะโดยอัตโนมัติ

### 3.6 ย้ายแหล่งข้อมูลจริงออกจาก "ไฟล์เดี่ยวบนเครื่องเดียว"
`TikTok_Live_Tracker.xlsx` ถ้ายังเป็นไฟล์ local จะ fork ได้ทันทีที่รันจากสองเครื่อง ทางเลือก
เรียงจากง่ายไปยาก:
1. **ง่ายสุด แก้ทันที** — วางไฟล์ไว้บน cloud storage ที่ sync เอง (Google Drive/OneDrive) แทนที่จะ
   อยู่บน SSD เฉยๆ แล้ว agent `tiktok-tracker-update` ยังเขียนไฟล์นี้เหมือนเดิมทุกอย่าง (มันสำรอง
   ไฟล์ + เช็ควันที่ซ้ำอยู่แล้วตาม `03`) — แค่เปลี่ยน path ที่ชี้ไป
2. **ยั่งยืนกว่า** — ย้ายให้ Notion (ที่ Phase 2 คุยกับอยู่แล้ว) เป็นแหล่งความจริงแทน `.xlsx` ไปเลย
   เพราะ Notion เป็น cloud-native อยู่แล้ว ไม่มีปัญหา fork ข้ามเครื่อง — แต่เป็นงานปรับ
   `tiktok-tracker-update` ใหม่ ไม่ใช่แค่ย้าย path

ไม่ว่าเลือกทางไหน **กฎเดิมต้องคงอยู่**: agent เขียนได้ตัวเดียว (`tiktok-tracker-update`), Phase 2
อ่านอย่างเดียว ห้ามเขียนกลับ

### 3.7 ความลับ (secrets)
Notion API token และ credential ของ MCP อื่นๆ ต้อง**ไม่**อยู่ใน repo เด็ดขาด — ใช้ `.env` ต่อเครื่อง
(อยู่ใน `.gitignore`) โดยมี `.env.example` เป็นเทมเพลตว่าต้องตั้งตัวแปรอะไรบ้าง ส่วนชื่อ MCP server
(เช่น `mcp__claude_ai_Notion__*`) **อย่า hardcode ในตัว agent** — ตามที่ `03`/`04` แนะนำไว้แล้วว่าให้
ลบบรรทัด `tools:` ทิ้งให้ agent inherit จาก session แทน เพราะชื่อ server เปลี่ยนได้ตามเครื่อง

### 3.8 ติดตั้ง skills/agents แบบอัตโนมัติ
แทนที่จะ copy ไฟล์เข้า `~/.claude/skills/` ด้วยมือทุกเครื่อง ให้ทำสคริปต์ติดตั้ง (หรือใช้ symlink)
ที่ลิงก์จาก `<repo>/.claude/skills/*` และ `<repo>/.claude/agents/*` เข้า `~/.claude/skills/` และ
`~/.claude/agents/` ของเครื่องนั้น วิธีนี้ทำให้ `git pull` ครั้งเดียวอัปเดตทุกเครื่องที่ตั้งไว้แล้ว
โดยไม่ต้อง copy มือซ้ำ

---

## 4. ส่วนที่ **ย้ายเป็น multi-machine เต็มรูปแบบไม่ได้จริง**

`tiktok-replay-fetch` ถูกออกแบบเป็น "click-and-exit" ที่คุยกับ Chrome จริงผ่าน Claude in Chrome MCP
โดยอาศัยว่า **ผู้ใช้ล็อกอิน TikTok ค้างไว้ในเบราว์เซอร์ของเครื่องนั้น** (ตามที่ `tiktok-replay-fetch.md`
เขียนไว้ชัดว่า agent ห้ามกรอกรหัสผ่าน/OTP แทนผู้ใช้เด็ดขาด) — การย้ายโค้ดขึ้น GitHub ทำให้
**ตัว agent** พร้อมใช้ทุกเครื่องที่ clone repo แต่ **ขั้นตอนกดโหลด replay ยังต้องรันบนเครื่องที่มี
Chrome ล็อกอิน TikTok ค้างอยู่จริงเท่านั้น** ไม่มีทางลัดที่ปลอดภัยกว่านี้โดยไม่เปลี่ยนสถาปัตยกรรม
ทั้งหมด (เช่น ต้องมีระบบ remote browser session ซึ่งเพิ่มความเสี่ยงเรื่อง credential แทน)

สรุปแนวทางที่ทำได้จริง: **แยกขั้นตอนดาวน์โหลด replay กับขั้นตอนถอดเสียง/ตัดคลิปออกจากกันไปเลย** —
เครื่องที่มี Chrome ล็อกอินอยู่ ทำหน้าที่โหลดไฟล์ลง SSD/cloud storage ที่ sync ได้ ส่วนขั้นถอดเสียง/
ตัดคลิป/เขียน tracker ทำได้จากเครื่องไหนก็ได้ที่มีไฟล์วิดีโอ + เครื่องมือครบ (นี่คือประโยชน์จริงที่
ได้จากการย้ายขึ้น GitHub — ไม่ใช่การทำให้ทั้งไปป์ไลน์ไร้เครื่องแบบ 100%)

---

## 5. ลำดับการทำแนะนำ

1. `git init` repo ใหม่ ย้าย `.claude/skills/*`, `.claude/agents/*`, สคริปต์ `.mjs` ทั้งหมดเข้าไป
   ก่อน (เป็นไฟล์ข้อความล้วน ย้ายตรงๆ ได้เลยไม่มีปัญหา)
2. ไล่แก้ path hardcode ในทุกสคริปต์ให้อ่านจาก env var ตามแบบที่ `vu.mjs` ทำไว้แล้ว
3. Vendor GSAP + เขียน `config/pinned-versions.json`
4. เขียน `install-mac.sh` (อิงจากขั้นตอนใน `01-คู่มือติดตั้ง.md` ที่มีอยู่แล้ว) และ
   `install-windows.ps1` (ของใหม่ ต้องทดสอบจริงบนเครื่อง Windows)
5. ตัดสินใจเรื่องแหล่งความจริงของ tracker (ข้อ 3.6) — เลือกทางที่ 1 ก่อนถ้าอยากเริ่มเร็ว
6. เขียน `README.md` เป็น "คู่มือ clone แล้วทำอะไรก่อน" — จะกลายเป็นเอกสารตัวที่ 7 ของชุดนี้
7. ทดสอบ dry-run บนเครื่องที่สอง (Windows หรือ Mac เครื่องอื่น) **ก่อน** ให้เครื่องนั้นแตะข้อมูลจริง
   หรือเขียนกลับ tracker จริง

---

## 6. เช็กลิสต์ก่อนถือว่า "พร้อมใช้ข้ามเครื่อง" จริง

- [ ] ไม่มี path ที่ hardcode `/Volumes/PortableSSD/CLAUDE edit` เหลืออยู่ในสคริปต์ไหนเลย
- [ ] โมเดล Whisper ดาวน์โหลดอัตโนมัติผ่านสคริปต์ติดตั้ง ไม่ใช่ copy มือ
- [ ] GSAP อ่านจากไฟล์ vendored ในโปรเจกต์ ไม่มีจุดไหนดึงจาก CDN แล้ว
- [ ] เวอร์ชัน HyperFrames ถูก pin ไว้ในไฟล์ config ที่ commit แล้ว
- [ ] `.env`/token ไม่ปรากฏใน git history เลย (เช็คด้วย `git log -p | grep -i token` ก่อน push)
- [ ] ทดสอบรัน `doctor`/`verify-doctor.mjs` ผ่านบนเครื่องที่สองแล้ว
- [ ] ตกลงกันแล้วว่า tracker อ่าน/เขียนจากที่เดียว (cloud path หรือ Notion) ไม่ fork ข้ามเครื่อง
- [ ] ทุกคนที่ใช้เครื่องต่างกันเข้าใจตรงกันว่า **ขั้นดาวน์โหลด replay ยังต้องรันบนเครื่องที่ Chrome
      ล็อกอิน TikTok ค้างไว้เท่านั้น**
